# Инструкция по подключению к GitHub

## ✅ Локальный репозиторий готов!

Git репозиторий инициализирован, все файлы добавлены и закоммичены.

## 📋 Следующие шаги:

### 1. Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `I-CHING` (или любое другое имя)
   - **Description**: "Минималистичное iOS-приложение для работы с И-цзин"
   - **Visibility**: Выберите Private или Public
   - **НЕ** создавайте README, .gitignore или license (они уже есть)
4. Нажмите **"Create repository"**

### 2. Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
cd "/Users/alexey/ PROG/I-CHING"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/I-CHING.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Или если используете SSH:**

```bash
git remote add origin git@github.com:YOUR_USERNAME/I-CHING.git
git branch -M main
git push -u origin main
```

### 3. Проверка

После выполнения команд:
- Обновите страницу репозитория на GitHub
- Все файлы должны быть видны
- Коммит "Initial commit: I-Ching iOS app project" должен быть в истории

## 🔄 Работа на другом компьютере

### Клонирование репозитория:

```bash
git clone https://github.com/YOUR_USERNAME/I-CHING.git
cd I-CHING
```

### Открытие в Cursor:

```bash
cursor I-CHING
```

Или через меню Cursor: **File → Open Folder**

## 📝 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить изменения с GitHub
git pull
```

## ⚠️ Важно

- Не коммитьте файлы из `.gitignore` (они автоматически игнорируются)
- Регулярно делайте `git push` чтобы сохранять изменения на GitHub
- На новом компьютере всегда делайте `git pull` перед началом работы

